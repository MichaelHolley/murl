## Documentation

- Prefer clear, concise, short instructions over excessive text.
- Changes to features, renames, restructuring, or deployment may require updating the docs (`README.md`, `docs/`). Only update them when the change actually diverges from what the docs describe — if nothing differs, leave them as is.