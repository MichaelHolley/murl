# Base URL of the murl service. Defaults to /api, which both the Vite dev
# server and the Docker nginx config forward to the service. Only set this
# when hosting the client separately from the service.
# VITE_SERVICE_URL=https://murl-api.example.com
